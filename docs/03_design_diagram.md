# 收集表设计图

## 页面结构

```mermaid
flowchart LR
  A["管理员端"] --> B["编辑"]
  A --> C["统计"]
  A --> D["设置"]

  B --> B1["标题与说明"]
  B --> B2["基础题目"]
  B --> B3["DOCX 文件题"]
  B --> B4["AI 脚本绑定"]
  B --> B5["预览与发布"]

  C --> C1["提交列表"]
  C --> C2["检查状态"]
  C --> C3["审核状态"]
  C --> C4["导出数据"]

  D --> D1["访问权限"]
  D --> D2["截止时间"]
  D --> D3["重复提交"]
```

## 用户提交流程

```mermaid
flowchart TD
  S["用户打开收集表链接"] --> F["填写基础信息"]
  F --> U["上传 DOCX 材料"]
  U --> V{"必填项与文件规则是否通过"}
  V -- "否" --> E["提示缺失项或格式错误"]
  E --> F
  V -- "是" --> P["提交"]
  P --> R["保存提交记录"]
  R --> O["上传原始 DOCX 到 OSS"]
  O --> Q["创建 AI DOCX 检查任务"]
  Q --> W["后台 Worker 执行检查"]
  W --> X["生成批注文档、评语和检查报告"]
  X --> T["更新检查状态"]
  T --> H["管理员统计页查看结果"]
```

## AI DOCX 处理链路

```mermaid
flowchart LR
  A["原始 DOCX"] --> B["脚本 1：基础格式检测"]
  B --> C["脚本 2：AI 内容评估"]
  C --> D["脚本 3：自动评语填写"]
  D --> E["批注文档"]
  D --> F["检查报告 JSON"]
  D --> G["运行日志"]

  E --> H["OSS 归档"]
  F --> H
  G --> H
  H --> I["统计页展示"]
```

## 模块关系

```mermaid
flowchart TB
  FE["前端 Web"] --> API["FastAPI 后端"]
  API --> DB["PostgreSQL"]
  API --> OSS["阿里云 OSS"]
  API --> TASK["AI 检查任务"]
  TASK --> WORKER["Python Worker"]
  WORKER --> OSS
  WORKER --> SCRIPT["用户配置的 Python 脚本"]
  SCRIPT --> AI["AI 服务"]
  SCRIPT --> OUT["批注与评语结果"]
  OUT --> OSS
  OUT --> DB
```

## 最小页面草图

```text
┌────────────────────────────────────────────────────────────┐
│ 收集表标题                                                  │
│ 说明文字 / 截止时间 / 提交规则                              │
├────────────────────────────────────────────────────────────┤
│ 题目 1：姓名 *                                             │
│ [________________________________]                         │
│                                                            │
│ 题目 2：学号 *                                             │
│ [________________________________]                         │
│                                                            │
│ 题目 3：上传毕业论文正文 DOCX *                            │
│ [ 选择文件 ] 仅支持 .docx                                  │
│                                                            │
│ 题目 4：补充说明                                           │
│ [________________________________]                         │
├────────────────────────────────────────────────────────────┤
│ [提交]                                                     │
└────────────────────────────────────────────────────────────┘
```

## 管理员统计页草图

```text
┌────────────────────────────────────────────────────────────┐
│ 统计：提交 42 份 | 待检查 3 | 检查失败 2 | 已通过 30          │
├──────────┬──────────┬──────────┬──────────┬───────────────┤
│ 姓名     │ 学号     │ 提交状态 │ 检查状态 │ 操作          │
├──────────┼──────────┼──────────┼──────────┼───────────────┤
│ 张三     │ 20240001 │ 已提交   │ 检查成功 │ 查看 / 下载   │
│ 李四     │ 20240002 │ 已提交   │ 检查中   │ 查看          │
│ 王五     │ 20240003 │ 已退回   │ 检查失败 │ 查看 / 退回   │
└──────────┴──────────┴──────────┴──────────┴───────────────┘
```
