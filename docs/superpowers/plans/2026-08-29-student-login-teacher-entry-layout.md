# 学生登录页教师入口位置调整计划

## 目标

将学生登录页的“教职工用户？教师登录 ›”从表单底部移动到“学生登录”标题上方，保持入口低强调、右对齐。

## 实施范围

- 调整 `AuthPortal.tsx` 中教师登录入口的渲染位置。
- 调整 `styles.css` 中该入口的间距与边框。
- 不修改登录路由、认证 API、权限校验或教师邀请机制。

## 验证边界

仅进行静态结构与样式审计；按照项目约定不运行测试、构建或浏览器自动化。完成后重启本地前后端并核对监听端口与工作目录。
