# 教务流程名称唯一性设计

## 规则

- 新建流程名称先去除首尾空格。
- 只要数据库中存在完全相同的流程名称，就拒绝创建并返回 `409`。
- 当前历史重复流程保持不变，不自动删除、合并或归档。
- 前端使用当前列表做即时校验，后端继续承担最终一致性校验。
- 冲突时创建弹窗保持打开，保留用户输入并显示“已存在同名流程”。

## 验证

- 后端测试覆盖同名拒绝、去除首尾空格后冲突、不同名称允许创建。
- 前端生产构建通过。
- 浏览器验证重复名称无法提交，修改为不同名称后可以创建。
