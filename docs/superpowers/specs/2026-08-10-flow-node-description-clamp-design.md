# 流程节点描述文字统一设计

## 目标

所有流程节点的描述文字统一使用 12 像素字号和两行高度，超出内容以省略号截断，避免描述贴近节点底部。

## 约束

- 标题继续使用现有常规、紧凑、密集三级字号。
- 描述不再随文字密度改变字号或行数。
- 节点尺寸继续固定为 `280 × 126` 像素。
- 不修改节点数据、连线和画布几何逻辑。

## 验收标准

1. 所有节点描述均为 12 像素。
2. 描述最多显示两行，超出后显示省略号。
3. 节点类型和状态区域保持独立，不与描述重叠。
