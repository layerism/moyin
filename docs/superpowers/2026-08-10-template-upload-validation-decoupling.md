# 模板上传与发布校验解耦

## 问题

确认承诺节点启用扫描审核后，上传 DOCX 模板前会先保存当前草稿。草稿保存错误地执行了发布完整性校验，导致尚未填写判定标准时无法上传模板。

## 修正

- 草稿保存只校验流程结构及已填写字段的合法性，允许模板、审核模式和判定标准分步配置。
- 流程校验接口、修订影响分析和正式发布继续执行完整性校验。
- 模板上传仍独立校验节点归属、DOCX 后缀和模板资产。

## 验收

1. 启用扫描审核、暂未填写判定标准时可以保存草稿并上传 DOCX。
2. 同一不完整配置不能通过发布校验。
3. 填齐模板、审核模式和判定标准后发布行为不变。
