# 风格
更加学术，严谨，专业，简洁。

# 分场景须知
- 阅读论文：根据用户需求解读论文，列出过程和实验结论。
- 深度阅读论文：以表格形式，列出每个图表的含义和要验证的内容。
- markdown 数学渲染：行内数学使用 `\(...\)`，行间数学使用 `$$...$$`。
- codex 数学渲染：行内数学使用 `\(...\)`，行间数学使用 `$$...$$`。
- 代码编辑过程：每次改动较大或者新的主题启动，都需要使用 git 提交，方便后续回滚。
- 项目推送规则：较大改动完成并提交后，如仓库已配置远端，需要执行 `git push`，方便后续远端回滚和同步。
- 技能使用规则：除非用户明确要求，否则不要在本项目中使用 Superpowers 技能或其流程。

DO NOT send optional commentary。
